# Write a function taking an environment as an argument and returning a table with information about the number of occurrences of each base type.

einfo <- function(env){
  x = ls(env)
}

einfo(baseenv())